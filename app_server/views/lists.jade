extends layout

block content
  .container
    .row
      .col-xs-12.col-md-6.col-md-offset-3
        h1.page-header= title
        if !items
          p No hay ningun item
        else
          ul
            each item in items
              li item.name
